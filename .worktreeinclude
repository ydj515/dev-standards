# Ignored local setup files copied into Codex-managed worktrees.
# Only matching Git-ignored files are copied; file contents stay untracked.

# Standards, templates, and composition configuration are tracked by Git.
# No ignored local runtime configuration needs to be copied.
# Add explicit ignored local setup paths here when introduced.
